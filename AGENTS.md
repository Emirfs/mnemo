# AGENTS.md

<!-- ghd:core:start -->
Work on a task branch. Use Conventional Commits. Never force-push, merge, or commit secrets, private vault data, indexes, and build outputs. Read `CONTEXT.md` before editing.
<!-- ghd:core:end -->

## This repository

- Install: `uv pip install -e ".[dev]"`
- Test: `uv run pytest`
- Public package contains no personal vault data.
